# Required — the app reads only this value (no hardcoded default). Copy to `.env`.
# Include the API version prefix used by admin routes.
# WARNING: Always use HTTPS in production to prevent token interception.
VITE_API_BASE_URL=https://api.mypharma.com/api/v1
